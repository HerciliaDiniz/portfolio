<div class="blog-post">
  <h2 class="blog-post-title"><%= link_to blog.title.upcase, blog %></h2>
  <p class="blog-post-meta">Published <%= distance_of_time_in_words(blog.created_at, Time.now) %> ago</a></p>
  <p class="blog-post-meta">
    <% if can? :manage, @blog %>
      <p><%= link_to blog.status, toggle_status_blog_path(blog), class: "btn btn-secondary" %></p>
    <% end %>
  </p>
  <p><%= blog.body %></p>
  <p class="blog-post-meta">
    <% if can? :manage, @blog %>
      <%= link_to 'Edit', edit_blog_path(blog), class: "btn btn-warning" %>
      <%= link_to 'Destroy', blog, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-danger" %>
    <% end %>
  </p>
</div>



