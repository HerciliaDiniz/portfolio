<div class="col-md-8 blog-main mt-5">

  <h2><strong><%= @blog.title %></strong></h2>
    <% if can? :manage, @blog %>
      <%= link_to 'Edit', edit_blog_path(@blog) %>
    <% end %>
  <p><%= @blog.body %></p>

</div>
